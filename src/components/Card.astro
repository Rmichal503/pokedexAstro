---
import {PokeapiRes} from '../types/pokeApi'
interface Props {
	pokemon_id: number | null
}
// `https://pokeapi.co/api/v2/pokemon/${searchString}/`

const { pokemon_id } = Astro.props;
const res:PokeapiRes = await fetch(`https://pokeapi.co/api/v2/pokemon/${pokemon_id}/`).then((r)=>{
	return r.json()
})
const stats
---
<div class="flex flex-col border border-slate-300 rounded-md w-[200px] h-fit p-4 items-center justify-center">
	<p class="text-2xl text-slate-600">{res.name}</p>
	<img src={res.sprites.front_default} alt={res.name}>
	<ul>

	</ul>
</div>

